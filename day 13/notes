1) JSX stands for JavaScript XML.
2) JSX allows us to write HTML in React.
3) JSX makes it easier to write and add HTML in React.
4) JSX allows us to write HTML elements in JavaScript and place them in the DOM without any createElement()  and/or appendChild() methods.
5) JSX converts HTML tags into react elements.